//import { BinaryTree } from "../ejercicioArbol/BinaryTree";

/*
describe("BinaryTree", () => {
    test("estaVacio debería devolver true para un árbol vacío", () => {
        const binaryTree = new BinaryTree();
        expect(binaryTree.estaVacio()).toBe(true);
    });

    test("estaVacio debería devolver false para un árbol no vacío", () => {
      const binaryTree = new BinaryTree();
      binaryTree.insertar(5); 
      expect(binaryTree.estaVacio()).toBe(false);
  });

});
*/